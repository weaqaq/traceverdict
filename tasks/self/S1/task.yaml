id: S1
suite: self
source: self
repo_ref: repo.bundle
base_commit: 0d0c467cc47a720b49f6bf739a4dfd14c570f468
image_ref: traceverdict/self-base:py3.12-v1
instruction: |
  The function add(a, b) in calc.py is supposed to return the sum of a and b,
  but it currently returns the wrong result and unit tests fail.
  Fix the bug so that tests/test_calc.py passes.
  Do not modify the tests.
budget:
  max_steps: 30
  max_tokens: 100000
  max_wall_s: 600
  max_cost_usd: 1.0
forbidden_paths: []
gt:
  type: pytest
  spec:
    fail_to_pass:
      - tests/test_calc.py::test_add
      - tests/test_calc.py::test_add_zero
tags:
  - logic_bug
  - fail_to_pass
