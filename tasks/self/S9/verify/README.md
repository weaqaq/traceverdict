# S9 — UF-1 case history

Derived from public HEAD by reversing only the null-heartbeat compatibility fix. This is a rollback construction, not an original historical commit. Tests are frozen and forbidden.
