# TraceVerdict 0.3.0 - Radar

- Adds the one-shot `radar` command family while leaving scheduling to the OS.
- Adds signal/confirmed alert levels and targeted three-run confirmation to Radar and Daily quick mode.
- Adds an explicit UTC-month API budget and seeded cumulative project-spend check without changing the frozen SQLite schema.
- Fixes negative passive-ingest open counts with content-free diagnostics.
- Extends full Daily Mode to S1-S11 with three explicitly rollback-constructed public case histories.

S9-S11 are derived from accepted public fixes for reproducible self-evaluation. They do not claim to be original historical commits.
